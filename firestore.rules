rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone can read and manage the lubricant product catalog
    match /products/{productId} {
      allow read, write: if true;
    }
  }
}

